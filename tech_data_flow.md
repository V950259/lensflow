# 5.4 数据流转与处理机制

本系统采用端云协同的混合处理架构，数据流转过程严格遵循“感知-认知-决策”的三层模型，确保在提供智能化服务的同时保障业务逻辑的确定性与安全性。完整数据流转过程如下：

**1. 图像采集与预处理层**
用户通过前端（Streamlit Web 端或 Android 移动端）触发图像采集指令。系统首先调用设备摄像头或文件系统获取原始图像流。在上传至服务端前，`app.py` 中的预处理模块会自动对图像进行格式校验与压缩，并调用 `base64` 编码器将其转换为标准的数据字符串。此过程在本地毫秒级完成，确保传输效率。

**2. 多模态语义解析层 (云端 AI)**
编码后的图像数据被封装进 API 请求体，通过 `LensAI` 客户端发送至多模态大模型服务（如 GLM-4v）。模型依据预置的 System Prompt 对图像进行深度感知，识别场景类型（海报/食品/通用），并提取关键实体信息（如活动时间、食品成分表）。模型输出被强制约束为标准化的 JSON 格式，解决了非结构化数据向结构化数据转化的核心难题。

**3. 逻辑规则过滤层 (本地 LogicEngine)**
获得 AI 解析的 JSON 数据后，系统并不直接采信，而是将其传入本地的确定性逻辑引擎 `LogicEngine`。
*   **健康风控**: 引擎加载 `data/allergens.json` 中的规则库，对 AI 提取的“成分列表”进行本地模糊匹配。一旦命中“海鲜”、“花生”等高危过敏原关键词，系统将强制覆盖 AI 的输出，生成不可被忽略的红色警示信号。
*   **协议标准化**: 针对时间信息，引擎通过 `EventParser` 将自然语言（如“明天下午”）解析为标准的 Python `datetime` 对象，并构建符合 RFC 5545 标准的 iCalendar 数据结构。

**4. UI 渲染与交互层**
经过双重处理的数据最终返回前端。UI 层根据场景类型动态渲染组件：对于海报场景，渲染日历下载按钮；对于食品场景，渲染带有颜色编码的风险标签。系统同时记录各阶段耗时（API 响应、规则匹配），实时反馈给用户，形成完整的交互闭环。
